// assign value of username to prompt command
var username = prompt(); 

// log to the whole page
alert(username);

// alert but better, because log in console
console.log(`the username is "${username}"`);